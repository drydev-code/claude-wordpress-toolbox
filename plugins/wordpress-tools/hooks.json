{
  "hooks": [
    {
      "event": "post-install",
      "command": "cd ${CLAUDE_PLUGIN_ROOT}/skills/wp-export && npm install --silent",
      "description": "Install wp-export dependencies"
    },
    {
      "event": "post-install",
      "command": "cd ${CLAUDE_PLUGIN_ROOT}/skills/wp-import && npm install --silent",
      "description": "Install wp-import dependencies"
    }
  ]
}
